id: example
namespace: org.kestra.templates

tasks:
- id: date
  type: org.kestra.task.templates.Example
  format: "{{taskrun.startDate}}"
- id: task-id
  type: org.kestra.task.templates.Example
  format: "{{task.id}}"
- id: flow-id
  type: org.kestra.task.templates.Example
  format: "{{outputs.task-id.child.value}}"
